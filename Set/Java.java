import java.util.Set;
import java.util.HashSet;

public class Main {
    public static void main(String[] args) {
        Set<ValueType> mySet = new HashSet<>();

        // Insertion
        mySet.add(value);

        // Search
        if (mySet.contains(value)) {
            // Value found
        }

        // Deletion
        mySet.remove(value);

        // Size
        int num_elements = mySet.size();

        // Iterate
        for (ValueType element : mySet) {
            // Process element
        }

        Set<ValueType> mySet = new HashSet<>();
        if (mySet.isEmpty()) {
            // The set is empty
        }
    }
}
