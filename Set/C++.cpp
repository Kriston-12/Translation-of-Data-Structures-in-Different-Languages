std::unordered_set<valueType> myset;

// Insertion
myset.insert(values)


// Search or Check value exists
if (myset.find(value) != myset.end()){}

// Delete
myset.erase(value)

// Size
size_t num_elements = mySet.size();

// Iterate
for (const auto& element : mySet) {
    // Process element
}

std::map<KeyType, ValueType> myMap;
if (myMap.empty()) {
    // The map is empty
}


